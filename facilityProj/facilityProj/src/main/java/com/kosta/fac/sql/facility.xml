<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.facility">
	
	<!-- 시설 전체 목록 조회 -->
	<select id="selectFacList" resultType="facility">
		select 
			fac_id as id, fac_type as type, fac_type_name as typeName,
			fac_nm as name, price, available_time as time, etc
		from facility
	</select>
	
	<!-- 시설 한개 조회 -->
	<select id="selectFacility" parameterType="String" resultType="facility">
		select 
			fac_id as id, fac_type as type, fac_type_name as typeName,
			fac_nm as name, price, available_time as time, etc
		from facility
		where fac_id = #{id}
	</select>
	
	<!-- 시설 선택 조회 -->
	<update id="updateFacility" parameterType="facility">
		update facility set 
					fac_nm = #{name},
					price = #{price},
					etc = #{etc}
		where fac_id = #{id}
	</update>
	
</mapper>	